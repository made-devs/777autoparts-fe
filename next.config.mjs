/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  images: {
    domains: ["pngimg.com", "images.unsplash.com"],
  },
  reactCompiler: true,
};

export default nextConfig;
